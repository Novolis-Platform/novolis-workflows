name: dotnet-build
description: Setup SDK, optional GitHub Packages auth, restore, build, and test
inputs:
  dotnet-version:
    description: .NET SDK version
    required: false
    default: '10.0.x'
  solution:
    description: Solution path (defaults to first *.slnx in repo root)
    required: false
    default: ''
  run_tests:
    description: Run dotnet test after build
    required: false
    default: 'true'
  packages_token:
    description: Optional read:packages token for private cross-repo restore
    required: false
    default: ''
  skip_dogfood_prepare:
    description: Set MSBuild SkipDogfoodPrepare (novolis-dogfooding)
    required: false
    default: 'false'
runs:
  using: composite
  steps:
    - uses: Novolis-Platform/novolis-workflows/actions/checkout-sibling-repos@main

    - name: Checkout governance build targets (sibling layout for MSBuild imports)
      shell: bash
      run: |
        set -euo pipefail
        gov="../novolis-governance"
        if [ ! -f "$gov/build/Novolis.Version.targets" ]; then
          git clone --depth 1 --branch main \
            "https://github.com/${GITHUB_REPOSITORY_OWNER}/novolis-governance.git" "$gov"
        fi
      env:
        GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}

    - uses: Novolis-Platform/novolis-workflows/actions/setup-dotnet@main
      with:
        dotnet-version: ${{ inputs.dotnet-version }}

    - uses: Novolis-Platform/novolis-workflows/actions/authenticate-github-packages@main
      with:
        token: ${{ inputs.packages_token != '' && inputs.packages_token || github.token }}

    - name: Restore, build, and test
      shell: bash
      env:
        SOLUTION: ${{ inputs.solution }}
        RUN_TESTS: ${{ inputs.run_tests }}
        SKIP_DOGFOOD: ${{ inputs.skip_dogfood_prepare }}
      run: |
        set -euo pipefail
        msbuild_props=()
        if [ "$SKIP_DOGFOOD" = "true" ]; then
          msbuild_props=(-p:SkipDogfoodPrepare=true)
        fi
        cfg_args=()
        if [ -f nuget.config ]; then
          cfg_args=(--configfile nuget.config)
        elif [ -f NuGet.config ]; then
          cfg_args=(--configfile NuGet.config)
        fi

        slnx="${SOLUTION:-}"
        if [ -z "$slnx" ]; then
          for f in *.slnx; do
            if [ -f "$f" ]; then slnx="$f"; break; fi
          done
        fi

        if [ -n "$slnx" ]; then
          dotnet restore "$slnx" "${cfg_args[@]}" "${msbuild_props[@]}"
          dotnet build "$slnx" --no-restore -c Release "${cfg_args[@]}" "${msbuild_props[@]}"
        else
          dotnet restore "${cfg_args[@]}" "${msbuild_props[@]}"
          dotnet build --no-restore -c Release "${cfg_args[@]}" "${msbuild_props[@]}"
        fi

        if [ "$RUN_TESTS" = "true" ]; then
          shopt -s nullglob
          ran=false
          for proj in tests/*/*.csproj tests/**/*.csproj; do
            [ -f "$proj" ] || continue
            is_test="$(dotnet msbuild "$proj" -getProperty:IsTestProject -nologo -v:q 2>/dev/null | tr -d '[:space:]')"
            if [ "$is_test" != "true" ]; then
              continue
            fi
            ran=true
            echo "Testing $proj"
            dotnet test "$proj" --no-build -c Release "${cfg_args[@]}"
          done
          if [ "$ran" = false ]; then
            echo "No test projects under tests/; skipping dotnet test."
          fi
        fi
