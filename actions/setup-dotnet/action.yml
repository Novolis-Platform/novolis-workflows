name: setup-dotnet
description: Setup .NET SDK
inputs:
  dotnet-version:
    required: false
    default: '10.0.x'
runs:
  using: composite
  steps:
    - uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ inputs.dotnet-version }}
