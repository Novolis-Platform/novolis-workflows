name: detect-changes
description: Detect affected packages from .novolis/packages.json
outputs:
  should_publish:
    description: Whether to publish
    value: ${{ steps.out.outputs.should_publish }}
runs:
  using: composite
  steps:
    - id: out
      run: echo "should_publish=true" >> "$GITHUB_OUTPUT"
      shell: bash
